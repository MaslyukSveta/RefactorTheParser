/**
 * Interface for representing file content
 * @typedef {Object} IFileContent
 * @property {string} message - The message
 * @property {string} timestamp - The timestamp
 */
export interface IFileContent {
    message: string;
    timestamp: string;
}
